services:
  backend:
    container_name: backend
    image: ${BACKEND_IMAGE}:${IMAGE_TAG}

    ports:
      - "5000:5000"

    env_file:
      - ./server/.env

    restart: unless-stopped

    networks:
      - virtualassisstant-network

  frontend:
    container_name: frontend
    image: ${FRONTEND_IMAGE}:${IMAGE_TAG}

    ports:
      - "3000:80"

    depends_on:
      - backend

    restart: unless-stopped

    networks:
      - virtualassisstant-network

networks:
  virtualassisstant-network:
    driver: bridge